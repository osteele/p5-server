# p5.js Libraries for Automatic Inclusion

The [p5-server][p5-server] command-line tool, and the [P5 Server Visual Studio
Code extension][p5-server-vscode-extensions], automatically include these libraries
when a sketch uses a function or `p5` property that they define. See
[automatic library includes][automatic-library-inclusion] for more
information.

Each library is listed with the symbols (functions, classes, and variables), and
the `p5` properties, that it defines.
A JavaScript-only sketch that *uses* one of these symbols or properties, and
does not *define* it, causes the inclusion of the library that defines that
symbol.

This page is generated from the files in [this directory][library-definitions].

[p5-server]: https://osteele.github.io/p5-server/
[p5-server-vscode-extensions]: https://marketplace.visualstudio.com/items?itemName=osteele.p5-server
[automatic-library-inclusion]: https://github.com/osteele/p5-server/tree/main/p5-analysis#automatic-library-inclusion
[library-definitions]: https://github.com/osteele/p5-server/tree/main/p5-analysis/src/models/libraries

{% for category in categories %}

## {{ category.name }} Libraries

<em>{{ category.description }}</em>

{{ category.details }}

{% for lib in category.libraries -%}

### [{{ lib.name }}]({{ lib.homepage }})

{{ lib.description }}

Defines:

{% for s in lib.defines.globals %}
- `{{ s }}`
{%- endfor -%}
{% for s in lib.defines.p5 %}
- `p5.{{ s }}`
{%- endfor %}

{% endfor %}

{% endfor %}
