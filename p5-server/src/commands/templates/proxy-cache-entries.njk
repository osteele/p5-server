Cache size: {{ entries.length }} entries

{%- for entry in entries %}
{{ entry.originUrl }}
{%- if verbose %}
  Created: {{ formatTime(entry.created) }}
  Expires: {{ formatTime(entry.expires) }}
{%- endif -%}
{%- endfor -%}
