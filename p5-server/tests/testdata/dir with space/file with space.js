// Description: Tests a file with a space in its name, in a directory with a space in its name

function setup() {
  createCanvas(windowWidth, windowHeight);
}

function draw() {
  circle(mouseX, mouseY, 20);
}
